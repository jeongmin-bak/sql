
SELECT *
FROM EMP;

UPDATE emp
SET sal = 0
WHERE ename = 'JONES';

COMMIT;

-- 82
SELECT *
FROM EMP;

SELECT *
FROM DEPT;

ALTER TABLE EMP ADD LOC VARCHAR2(13);

ROLLBACK ;

MERGE INTO EMP e
USING DEPT d
ON (e.DEPTNO = d.DEPTNO)
WHEN MATCHED THEN
    UPDATE SET e.LOC = d.LOC
WHEN NOT MATCHED THEN
    INSERT (e.empno, e.deptno, e.loc) VALUES(1111, d.deptno, d.loc);

SELECT *
FROM EMP;

-- 82-1
ALTER TABLE EMP ADD DNAME VARCHAR2(10);

MERGE INTO EMP e
USING DEPT d
ON (e.DEPTNO = d.DEPTNO)
WHEN MATCHED THEN
    UPDATE SET e.DNAME = d.DNAME;

UPDATE emp
SET sal = 9000
WHERE ename = 'JONES';

COMMIT;

SELECT *
FROM EMP;

